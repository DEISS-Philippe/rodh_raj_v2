{% extends 'layout.html.twig' %}

{% block title %}
    <title>{{ 'app.project.homepage'|trans }}</title>
{% endblock %}

{% block body %}
    <div class="container_donjon">

            <div class="you_died">
                <div class="skullContainer">
                    <img src="{{ asset('assets/images/skull.svg') }}" alt="crane">
                </div>
                <div class="skullMessage text_center">
                    <p>You Died !</p>
                </div>
                <div class="skullComment">
                    <p>{{ deathNote }}</p>
                </div>
                <div class="skullBackLink">
                    <div class="menuLink bckg-grey skullLink">
                        <a href="{{ path('homepage') }}">{{ 'app.project.back_to_menu'|trans }}</a>
                    </div>
                </div>
            </div>

    </div>
{% endblock %}
